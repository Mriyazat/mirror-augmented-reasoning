# =============================================================================
# DDI Verifier — Pinned Python dependencies
# Two installation profiles:
#   1) Local / laptop (data prep, audits, small CPU experiments):
#        pip install -r requirements.txt
#   2) Cluster (training + inference with CUDA + vLLM):
#        Install GPU-wheel torch / vllm separately on a login node with
#        internet access. See scripts/cluster_examples/setup_env.sh.
# =============================================================================

# === Core numerical stack ===
numpy>=1.26
scipy>=1.13
pandas>=2.2
pyarrow>=17.0
scikit-learn>=1.5
xgboost>=2.0

# === XML + parsing ===
lxml>=5.3
beautifulsoup4>=4.12

# === NLP / lightweight reasoning parse for taxonomy construction ===
spacy==3.7.5
# Pull the small English model lazily:
#   python -m spacy download en_core_web_sm

# === LLM training / inference stack ===
# (CPU-usable install; swap in GPU-built wheels of torch / vllm on a cluster.)
torch==2.3.1
transformers==4.42.3
accelerate==0.32.1
peft==0.11.1
trl==0.9.4
datasets==2.20.0
sentencepiece==0.2.0
tokenizers==0.19.1

# === Graph / pathway retrieval ===
networkx==3.3

# === Config / IO / logging ===
pyyaml==6.0.1
tqdm==4.66.4
jsonlines==4.0.0
click==8.1.7
rich==13.7.1

# === Experiment tracking (optional) ===
# wandb==0.17.3

# === Test + lint ===
pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.0

# === Stats / calibration for abstention ===
statsmodels==0.14.2

# === Cheminformatics (retrieval audit) ===
# rdkit is used only by src/metrics/mor.py and is imported lazily, so the
# rest of the pipeline runs without it. Install if you want to reproduce
# the MOR (Mechanism-Overlap Ratio) retrieval audit.
#
# Install notes:
#   * Local: pip install 'rdkit>=2024.3'  (PyPI wheels from 2024.03+)
#   * HPC: some sites ship a placeholder wheel; in that case load RDKit
#     through the cluster module system BEFORE activating your venv. The
#     example launcher scripts/cluster_examples/activate_env.sh does this
#     with `module load ... rdkit/2024.09.6`.
rdkit>=2024.3; sys_platform != "linux" or platform_machine != "x86_64"

# Note: GNN baselines that rely on torch-geometric / torch-scatter /
# torch-sparse must be installed separately against the CUDA build of torch
# to avoid ABI mismatches.
